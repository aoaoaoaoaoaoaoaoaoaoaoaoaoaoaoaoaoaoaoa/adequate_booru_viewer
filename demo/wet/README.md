# wet demo

`cargo xtask wet-demo`

The demo stages only `config.toml` and `slate.toml`; the durable Danbooru index
and media cache come from the operator's normal XDG data/cache roots. That is
the intended covenant: reproducible choreography, not a fake aquarium.
