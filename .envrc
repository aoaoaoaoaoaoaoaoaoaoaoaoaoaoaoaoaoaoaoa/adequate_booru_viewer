# Optional machine-local target root; absent means Cargo's native default.
source_env_if_exists .envrc.local
if [[ -n "${CARGO_TARGET_ROOT:-}" ]]; then
    export CARGO_TARGET_DIR="${CARGO_TARGET_ROOT%/}/${PWD##*/}"
fi
